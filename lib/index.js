var server = require('./Server');
var client = require('./Client');

module.exports = {
	Server: server,
	Spider:	server,
	Tarantula: server,
	server: server,
	Client:	client
	client: client;
	Fly: client,
};